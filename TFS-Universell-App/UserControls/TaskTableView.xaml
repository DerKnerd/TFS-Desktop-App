<UserControl
    x:Class="TFS.Client.UserControls.TaskTableView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TFS.Client.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:MyToolkit.Controls"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400"
    Name="view">

    <Grid>
        <controls:DataGrid ItemsSource="{Binding ViewModel, ElementName=view}">
            <controls:DataGrid.Columns>
                <controls:DataGridTextColumn Header="Typ" Binding="{Binding Type}" />
                <controls:DataGridTextColumn Header="Titel" Binding="{Binding Title}" />
                <controls:DataGridTextColumn Header="Zustand" Binding="{Binding State}" />
                <controls:DataGridTextColumn Header="Aufwand" Binding="{Binding Effort}" />
                <controls:DataGridTextColumn Header="Iterationspfad" Binding="{Binding IterationPath}" />
            </controls:DataGrid.Columns>
            <controls:DataGrid.ItemDetailsTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Description}" />
                </DataTemplate>
            </controls:DataGrid.ItemDetailsTemplate>
        </controls:DataGrid>
    </Grid>
</UserControl>
