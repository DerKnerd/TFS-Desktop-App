<ContentDialog
    x:Class="TFS.Client.Dialogs.StartupDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TFS.Client.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="" x:Uid="StartupDialog"
    PrimaryButtonText=""
    SecondaryButtonText=""
    PrimaryButtonClick="ContentDialog_PrimaryButtonClick"
    SecondaryButtonClick="ContentDialog_SecondaryButtonClick"
    Name="View">
    <StackPanel>
        <TextBlock x:Uid="StartupDialogUrl" />
        <TextBox Text="{Binding ViewModel.Url, ElementName=View, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="Url" />
        <TextBlock x:Uid="StartupDialogLogin" />
        <TextBox Text="{Binding ViewModel.Username, ElementName=View, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="Text" />
        <TextBlock x:Uid="StartupDialogPassword" />
        <PasswordBox Password="{Binding ViewModel.Password, ElementName=View, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
        <TextBlock x:Uid="StartupDialogCollection"></TextBlock>
        <TextBox Text="{Binding ViewModel.Collection, ElementName=View, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" InputScope="Text" />
    </StackPanel>
</ContentDialog>